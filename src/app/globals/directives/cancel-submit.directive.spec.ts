import { CancelSubmitDirective } from './cancel-submit.directive';

describe('CancelSubmitDirective', () => {
  it('should create an instance', () => {
    const directive = new CancelSubmitDirective();
    expect(directive).toBeTruthy();
  });
});
