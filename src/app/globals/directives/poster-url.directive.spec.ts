import { PosterUrlDirective } from './poster-url.directive';

describe('PosterUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new PosterUrlDirective();
    expect(directive).toBeTruthy();
  });
});
